import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'where-is-my-stuff-frontend',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
